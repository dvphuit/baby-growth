name: Refactor domain ownership

on:
  push:
    branches:
      - refactor/feature-first-architecture
    paths:
      - .github/workflows/refactor-domain-ownership.yml
      - app/scripts/refactor-domain-ownership.mjs

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/feature-first-architecture
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: app/package-lock.json
      - name: Apply and validate domain ownership migration
        run: |
          set +e
          (
            cd app
            node scripts/refactor-domain-ownership.mjs
            npm ci
            npm test
            npm run lint
            npm run build
          ) 2>&1 | tee /tmp/refactor-domain-ownership.log
          status=${PIPESTATUS[0]}
          set -e
          exit "$status"
      - name: Report migration failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          {
            echo "Domain ownership codemod failed for \`$GITHUB_SHA\`."
            echo
            echo "<details><summary>Failure log tail</summary>"
            echo
            echo '```text'
            tail -n 140 /tmp/refactor-domain-ownership.log || true
            echo '```'
            echo "</details>"
          } > /tmp/refactor-domain-ownership-comment.md
          gh pr comment 16 --repo "$GITHUB_REPOSITORY" --body-file /tmp/refactor-domain-ownership-comment.md
      - name: Commit domain ownership migration
        if: success()
        run: |
          rm .github/workflows/refactor-domain-ownership.yml app/scripts/refactor-domain-ownership.mjs
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- app/src .github/workflows/refactor-domain-ownership.yml app/scripts/refactor-domain-ownership.mjs
          git diff --cached --check
          unexpected="$(git diff --cached --name-only | grep -Ev '^(app/src/|app/scripts/refactor-domain-ownership\.mjs$|\.github/workflows/refactor-domain-ownership\.yml$)' || true)"
          if [ -n "$unexpected" ]; then
            echo "Unexpected staged paths:"
            echo "$unexpected"
            exit 1
          fi
          git commit -m "refactor: colocate domain state and lifecycle"
          git push origin HEAD:refactor/feature-first-architecture
